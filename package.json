{
  "private": true,
  "workspaces": [
    "packages/*",
    "*"
  ],
  "scripts": {
    "dev": "lerna run dev --parallel",
    "test": "lerna run test",
    "build": "lerna run build"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "lerna": "^8.1.2",
    "typescript": "^5.4.0"
  }
}
