{
  "private": true,
  "workspaces": [
    "packages/*",
    "*"
  ],
  "scripts": {
    "dev": "lerna run dev --parallel",
    "storybook": "lerna run build:watch,storybook",
    "test": "lerna run test",
    "build": "lerna run build --concurrency 1"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "lerna": "^8.1.2",
    "typescript": "^5.4.0"
  }
}
